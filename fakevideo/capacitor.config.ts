import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fakevideo.meet',
  appName: 'GoMeet',
  webDir: 'dist/fakevideo/browser'
};

export default config;
